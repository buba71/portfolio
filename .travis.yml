language: php
php:
- '7.2.1'

branches:
  only:
    - develop

sudo: false

services: postgresql

before_install:
- composer self-update

install:
- composer install
- cp .env.dist .env
- php bin/console doctrine:database:create
- php bin/console doctrine:schema:create

script:
  - php bin/phpunit